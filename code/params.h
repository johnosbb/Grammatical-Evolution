#define CODON		8	// codon size in bits
#define COMPILE		0 	// Use compiler (1) or interpreter (0)
//#define WRAP		1	// Turn wrapping on(1) or off(0)
#define WRAP_EVENTS	10	// Upper-bound for Wrapping events
#define LENGTH_OF_EXPRESSION 200 // max. no. of elements in mapped expr
				  // limited only by memory! was 2000 
                                 // typically set to 200
// was 1000 for spirals



